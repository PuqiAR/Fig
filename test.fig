struct Person
{
    name: String;
    age: Int = 10;

    public func getName()
    {
        return name;
    }
}

var person = Person{"123"};

const print := __fstdout_print;
print(person.name);
person.name = "sb";
print(person.name);